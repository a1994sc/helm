apiVersion: v1
kind: Service
metadata:
  name: {{ include "tekton-pipeline.fullname" . }}-remote-resolvers
  labels:
    app: tekton-pipelines-remote-resolvers
    app.kubernetes.io/component: resolvers
    app.kubernetes.io/part-of: tekton-pipelines
    pipeline.tekton.dev/release: v0.54.0
    version: v0.54.0
  {{- include "tekton-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.remoteResolvers.type }}
  selector:
    app.kubernetes.io/component: resolvers
    app.kubernetes.io/instance: default
    app.kubernetes.io/name: resolvers
    app.kubernetes.io/part-of: tekton-pipelines
  {{- include "tekton-pipeline.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.remoteResolvers.ports | toYaml | nindent 2 -}}
