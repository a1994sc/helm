apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tekton-dashboard.fullname" . }}-dashboard-info
  labels:
    app.kubernetes.io/part-of: tekton-dashboard
  {{- include "tekton-dashboard.labels" . | nindent 4 }}
data:
  version: {{ .Values.dashboardInfo.version | quote }}
